{
  "name": "photo-gallery-bhel",
  "version": "1.0.0",
  "description": "Photo Gallery BHEL - A comprehensive web application for managing and displaying photo collections",
  "private": true,
  "workspaces": [
    "src",
    "admin-portal",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd src && npm run dev",
    "dev:backend": "cd server && npm run dev",
    "dev:admin": "cd admin-portal && npm start",
    "build": "npm run build:frontend && npm run build:admin && npm run build:backend",
    "build:frontend": "cd src && npm run build",
    "build:admin": "cd admin-portal && npm run build",
    "build:backend": "cd server && npm run build",
    "install:all": "npm install && cd src && npm install && cd ../admin-portal && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "photo-gallery",
    "react",
    "nodejs",
    "mongodb",
    "typescript",
    "material-ui"
  ],
  "author": "BHEL Team",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "docx": "^9.5.1",
    "mongoose": "^8.16.1",
    "xlsx": "^0.18.5"
  }
}
